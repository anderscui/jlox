// if
var i = 1;
var j = 1;

if (i >= 0 and j > 0) {
    print "both positive";
}

// while
var i = 1;
var sum = 0;
while (i <= 100) {
    sum = sum + i;
    i = i + 1;
}
print sum;

// for: fib series.
var a = 0;
var b = 1;
var temp;
for (; a < 10; b = temp + b) {
    print a;
    temp = a;
    a = b;
}
